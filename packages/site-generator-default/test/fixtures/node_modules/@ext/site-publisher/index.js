'use strict'

const delegate = require('@antora/site-publisher')

const pings = new Set()

async function publishSite (catalogs, context) {
  const result = await delegate(catalogs, context)
  result.push(pings)
  return result
}

module.exports = publishSite
module.exports.ping = (name) => pings.add(name)
